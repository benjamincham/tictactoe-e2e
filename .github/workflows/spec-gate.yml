name: Spec Gate
on:
  pull_request:
    paths:
      - "specs/**"
jobs:
  lint-tasks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint tasks.md (SDD standard)
        run: python3 scripts/lint_tasks.py specs
